<div id="successtoast" class="successtoast">
  <div>
    <strong id="success">Salvo com sucesso!</strong>
  </div>
</div>


<script>
  // This function is scheduled to run 1 seconds after the page loads.
  
    setTimeout(function () {
      document.getElementById('successtoast').animate([
        { transform: 'translateY(0px)' },
        { transform: 'translateX(300px)' }
      ], {
        duration: 4000,
      }
      )
    }, 200);

    // This function is scheduled to run 1 seconds after the page loads.
    setTimeout(function () {
      document.getElementById('successtoast').style.display = 'none';
    }, 1000);
  
</script>